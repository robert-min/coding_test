def solution(n, lost, reserve):
    answer = 0
    # 학생수 n, 잃어버린 학생 번호 리스트 lost, 여분 옷가진 학생 번호 reserve
    # 최대한 많은 학생이 수업 참여하도록 학생 최대값 리턴
    # 잃어버린 학생 앞 뒤에 여분 있는지 => 있으면 그대로, 없으면 -1
    # 본인도 잃어버릴 수 있음
    # 빌려줬을 때 리스트도 확인
    
        
    
    return len(answer)

print(solution(5, [2, 4], [3]))